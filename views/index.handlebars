{{!--
<div class="container">
  <div class="intro">


    <h1>Ice Cream Eater</h1>
    <p>Enter your favorite flavor. Then, click to devour it!</p>
  </div>
  <div class="uneaten-flavors">
    <h2>Uneaten ice cream flavors:</h2>

    <ul>
      {{#each flavors}} <br> {{#unless eaten}} {{> flavors/flavor-block eaten=false}} {{/unless}} {{/each}}
    </ul>
  </div>
  <div class="eaten-flavors">
    <h2>You ate these!</h2>

    <ul>
      {{#each flavors}} {{#if eaten}} {{> flavors/flavor-block eaten=true}}
      <li class="cone">
        {{cone_name}}
      </li>
      {{/if}} {{/each}}
    </ul>
  </div>
  <div class="add-flavor">
    <h2>Add a flavor to the list</h2><br>
    <form class="create-form">

      <div class="form-group">
        <label for="ca">Flavor Name:</label><br>
        <input type="text" id="ca" name="name">
      </div>
      <br>
      <button type="submit" class="submit-flavor">Add My Flavor!</button>
    </form>

    {{!-- new 9/12/18 --}}
    <form class="devour-form button-size">
      <input input type="hidden" class="icecream_id" type="text" value={{this.id}}><br>
      <button type="submit" class="btn btn-default">Devour it!</button>
    </form>


  </div>
</div> --}} {{!-- new 9/12/18 --}}

<div class="container">
  <div class="row">
    <div class="col-12">
    <div class="intro">
      <h1>Ice Cream Eater</h1>
      <p>Enter your favorite flavor. Then, click to devour it!</p>
    </div>
    </div>
    </div>
    <div class="row">
      <div class="col-6 uneaten-flavors">
      <h2>Uneaten ice cream flavors:</h2>
      <ul>
        {{#each flavors_data}} {{#unless this.eaten}} 
       <pre> {{this.id}}. {{this.cone_name}} </pre>
      </ul>
      <form class="devour-form button-size">
        <input input type="hidden" class="icecream_id" type="text" value={{this.id}}><br>
        <button type="submit" class="btn btn-default">Devour it!</button>
      </form>
    </div>
    {{/unless}} {{/each}}
  
  <div class="col-6 text-center" class="task">
    {{#each flavor_data}} {{#if this.eaten}}
    <input class="form-control" type="text" placeholder="{{this.id}} . {{this.cone_name}}">
  </div>
  {{/if}} {{/each}}
  </div>
  <div class="row">
     <div class="col-12 add-flavor">
    <h2>Add a flavor to the list</h2><br>
    <form action="/flavors/create" method="POST">
      {{!-- or maybe instead of flavor_name it should be icecream_name --}}
      <input type="text" class="form-control" name="cone_name" value="" id="enter_text">
      <button type="submit" class="btn" id="submit-flavor">Add My Flavor!</button>
    </form>
</div>
</div>
</div>

